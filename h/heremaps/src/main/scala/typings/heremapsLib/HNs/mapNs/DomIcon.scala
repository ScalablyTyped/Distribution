package typings
package heremapsLib.HNs.mapNs

import scala.scalajs.js
import scala.scalajs.js.`|`
import scala.scalajs.js.annotation._

/**
         * A visual representation of the H.map.DomMarker.
         */
@JSGlobal("H.map.DomIcon")
@js.native
class DomIcon protected () extends js.Object {
  /**
               * Constructor
               * @param element {!(Element | string)} - The element or markup to use for this icon
               * @param opt_options {H.map.DomIcon.Options=}
               */
  def this(element: java.lang.String) = this()
  /**
               * Constructor
               * @param element {!(Element | string)} - The element or markup to use for this icon
               * @param opt_options {H.map.DomIcon.Options=}
               */
  def this(element: stdLib.Element) = this()
  /**
               * Constructor
               * @param element {!(Element | string)} - The element or markup to use for this icon
               * @param opt_options {H.map.DomIcon.Options=}
               */
  def this(element: java.lang.String, opt_options: heremapsLib.HNs.mapNs.DomIconNs.Options) = this()
  /**
               * Constructor
               * @param element {!(Element | string)} - The element or markup to use for this icon
               * @param opt_options {H.map.DomIcon.Options=}
               */
  def this(element: stdLib.Element, opt_options: heremapsLib.HNs.mapNs.DomIconNs.Options) = this()
}

