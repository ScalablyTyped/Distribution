package typings.semanticDashUiDashDropdown.SemanticUI.Dropdown.ClassNameSettings

import scala.scalajs.js
import scala.scalajs.js.`|`
import scala.scalajs.js.annotation._

trait _Impl extends js.Object {
  /**
    * @default 'active'
    */
  var active: String
  /**
    * @default 'addition'
    */
  var addition: String
  /**
    * @default 'animating'
    */
  var animating: String
  /**
    * @default 'disabled'
    */
  var disabled: String
  /**
    * @default 'ui dropdown'
    */
  var dropdown: String
  /**
    * @default 'filtered'
    */
  var filtered: String
  /**
    * @default 'hidden transition'
    */
  var hidden: String
  /**
    * @default 'item'
    */
  var item: String
  /**
    * @default 'ui label'
    */
  var label: String
  /**
    * @default 'loading'
    */
  var loading: String
  /**
    * @default 'menu'
    */
  var menu: String
  /**
    * @default 'message'
    */
  var message: String
  /**
    * @default 'multiple'
    */
  var multiple: String
  /**
    * @default 'default'
    */
  var placeholder: String
  /**
    * @default 'search'
    */
  var search: String
  /**
    * @default 'selected'
    */
  var selected: String
  /**
    * @default 'selection'
    */
  var selection: String
  /**
    * @default 'upward'
    */
  var upward: String
  /**
    * @default 'visible'
    */
  var visible: String
}

object _Impl {
  @scala.inline
  def apply(
    active: String,
    addition: String,
    animating: String,
    disabled: String,
    dropdown: String,
    filtered: String,
    hidden: String,
    item: String,
    label: String,
    loading: String,
    menu: String,
    message: String,
    multiple: String,
    placeholder: String,
    search: String,
    selected: String,
    selection: String,
    upward: String,
    visible: String
  ): _Impl = {
    val __obj = js.Dynamic.literal(active = active, addition = addition, animating = animating, disabled = disabled, dropdown = dropdown, filtered = filtered, hidden = hidden, item = item, label = label, loading = loading, menu = menu, message = message, multiple = multiple, placeholder = placeholder, search = search, selected = selected, selection = selection, upward = upward, visible = visible)
  
    __obj.asInstanceOf[_Impl]
  }
}

