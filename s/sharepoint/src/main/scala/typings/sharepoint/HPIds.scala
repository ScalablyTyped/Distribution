package typings.sharepoint

import scala.scalajs.js
import scala.scalajs.js.`|`
import scala.scalajs.js.annotation._

trait HPIds extends js.Object {
  var actions: String
  var arrow: String
  var arrowBorder: String
  var author: String
  var body: String
  var close: String
  var content: String
  var dateCreated: String
  var dimensions: String
  var fileType: String
  var follow: String
  var header: String
  var inner: String
  var modifiedDate: String
  var noData: String
  var open: String
  var openClient: String
  var parentLink: String
  var peopleInterests: String
  var peopleMemberships: String
  var peoplePastProjects: String
  var peopleSchools: String
  var peopleSkills: String
  var peopleSummary: String
  var post: String
  var preview: String
  var sectionName: String
  var send: String
  var siteDescription: String
  var siteLogo: String
  var size: String
  var summary: String
  var title: String
  var viewDuplicates: String
  var viewer: String
  var viewsLifeTime: String
  var viewsRecent: String
}

object HPIds {
  @scala.inline
  def apply(
    actions: String,
    arrow: String,
    arrowBorder: String,
    author: String,
    body: String,
    close: String,
    content: String,
    dateCreated: String,
    dimensions: String,
    fileType: String,
    follow: String,
    header: String,
    inner: String,
    modifiedDate: String,
    noData: String,
    open: String,
    openClient: String,
    parentLink: String,
    peopleInterests: String,
    peopleMemberships: String,
    peoplePastProjects: String,
    peopleSchools: String,
    peopleSkills: String,
    peopleSummary: String,
    post: String,
    preview: String,
    sectionName: String,
    send: String,
    siteDescription: String,
    siteLogo: String,
    size: String,
    summary: String,
    title: String,
    viewDuplicates: String,
    viewer: String,
    viewsLifeTime: String,
    viewsRecent: String
  ): HPIds = {
    val __obj = js.Dynamic.literal(actions = actions, arrow = arrow, arrowBorder = arrowBorder, author = author, body = body, close = close, content = content, dateCreated = dateCreated, dimensions = dimensions, fileType = fileType, follow = follow, header = header, inner = inner, modifiedDate = modifiedDate, noData = noData, open = open, openClient = openClient, parentLink = parentLink, peopleInterests = peopleInterests, peopleMemberships = peopleMemberships, peoplePastProjects = peoplePastProjects, peopleSchools = peopleSchools, peopleSkills = peopleSkills, peopleSummary = peopleSummary, post = post, preview = preview, sectionName = sectionName, send = send, siteDescription = siteDescription, siteLogo = siteLogo, size = size, summary = summary, title = title, viewDuplicates = viewDuplicates, viewer = viewer, viewsLifeTime = viewsLifeTime, viewsRecent = viewsRecent)
  
    __obj.asInstanceOf[HPIds]
  }
}

