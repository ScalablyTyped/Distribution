package typings.lost

import org.scalablytyped.runtime.Shortcut
import typings.lost.mod.LostLibNoSettings
import org.scalablytyped.runtime.StObject
import scala.scalajs.js
import scala.scalajs.js.annotation.{JSGlobalScope, JSGlobal, JSImport, JSName, JSBracketAccess}

object libLostUtilityMod extends Shortcut {
  
  @JSImport("lost/lib/lost-utility", JSImport.Namespace)
  @js.native
  val ^ : LostLibNoSettings[Unit] = js.native
  
  type _To = LostLibNoSettings[Unit]
  
  /* This means you don't have to write `^`, but can instead just say `libLostUtilityMod.foo` */
  override def _to: LostLibNoSettings[Unit] = ^
}
