package typings.electron.Electron

import typings.node.eventsMod.EventEmitter
import org.scalablytyped.runtime.StObject
import scala.scalajs.js
import scala.scalajs.js.annotation.{JSGlobalScope, JSGlobal, JSImport, JSName, JSBracketAccess}


type Accelerator = String

/**
  * The `EventEmitter` class is defined and exposed by the `events` module:
  *
  * ```js
  * const EventEmitter = require('events');
  * ```
  *
  * All `EventEmitter`s emit the event `'newListener'` when new listeners are
  * added and `'removeListener'` when existing listeners are removed.
  *
  * It supports the following option:
  * @since v0.1.26
  */
/* was `typeof imported_events.EventEmitter` */
type NodeEventEmitter = EventEmitter

type TouchBarGroup = EventEmitter

type TouchBarOtherItemsProxy = EventEmitter

/* was `typeof NativeImage` */
type nativeImage = NativeImage_

/* was `typeof Session` */
type session = Session_

/* was `typeof WebContents` */
type webContents = WebContents_

/* was `typeof WebFrameMain` */
type webFrameMain = WebFrameMain_
