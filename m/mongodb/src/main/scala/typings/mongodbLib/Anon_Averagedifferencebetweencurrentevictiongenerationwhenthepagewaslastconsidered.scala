package typings
package mongodbLib

import scala.scalajs.js
import scala.scalajs.js.`|`
import scala.scalajs.js.annotation._

trait Anon_Averagedifferencebetweencurrentevictiongenerationwhenthepagewaslastconsidered extends js.Object {
  var `Average difference between current eviction generation when the page was last considered`: scala.Double
  var `Average on-disk page image size seen`: scala.Double
  var `Clean pages currently in cache`: scala.Double
  var `Current eviction generation`: scala.Double
  var `Dirty pages currently in cache`: scala.Double
  var `Entries in the root page`: scala.Double
  var `Internal pages currently in cache`: scala.Double
  var `Leaf pages currently in cache`: scala.Double
  var `Maximum difference between current eviction generation when the page was last considered`: scala.Double
  var `Maximum page size seen`: scala.Double
  var `Minimum on-disk page image size seen`: scala.Double
  var `On-disk page image sizes smaller than a single allocation unit`: scala.Double
  var `Pages created in memory and never written`: scala.Double
  var `Pages currently queued for eviction`: scala.Double
  var `Pages that could not be queued for eviction`: scala.Double
  var `Refs skipped during cache traversal`: scala.Double
  var `Size of the root page`: scala.Double
  var `Total number of pages currently in cache`: scala.Double
}

object Anon_Averagedifferencebetweencurrentevictiongenerationwhenthepagewaslastconsidered {
  @scala.inline
  def apply(
    `Average difference between current eviction generation when the page was last considered`: scala.Double,
    `Average on-disk page image size seen`: scala.Double,
    `Clean pages currently in cache`: scala.Double,
    `Current eviction generation`: scala.Double,
    `Dirty pages currently in cache`: scala.Double,
    `Entries in the root page`: scala.Double,
    `Internal pages currently in cache`: scala.Double,
    `Leaf pages currently in cache`: scala.Double,
    `Maximum difference between current eviction generation when the page was last considered`: scala.Double,
    `Maximum page size seen`: scala.Double,
    `Minimum on-disk page image size seen`: scala.Double,
    `On-disk page image sizes smaller than a single allocation unit`: scala.Double,
    `Pages created in memory and never written`: scala.Double,
    `Pages currently queued for eviction`: scala.Double,
    `Pages that could not be queued for eviction`: scala.Double,
    `Refs skipped during cache traversal`: scala.Double,
    `Size of the root page`: scala.Double,
    `Total number of pages currently in cache`: scala.Double
  ): Anon_Averagedifferencebetweencurrentevictiongenerationwhenthepagewaslastconsidered = {
    val __obj = js.Dynamic.literal()
    __obj.updateDynamic("Average difference between current eviction generation when the page was last considered")(`Average difference between current eviction generation when the page was last considered`)
    __obj.updateDynamic("Average on-disk page image size seen")(`Average on-disk page image size seen`)
    __obj.updateDynamic("Clean pages currently in cache")(`Clean pages currently in cache`)
    __obj.updateDynamic("Current eviction generation")(`Current eviction generation`)
    __obj.updateDynamic("Dirty pages currently in cache")(`Dirty pages currently in cache`)
    __obj.updateDynamic("Entries in the root page")(`Entries in the root page`)
    __obj.updateDynamic("Internal pages currently in cache")(`Internal pages currently in cache`)
    __obj.updateDynamic("Leaf pages currently in cache")(`Leaf pages currently in cache`)
    __obj.updateDynamic("Maximum difference between current eviction generation when the page was last considered")(`Maximum difference between current eviction generation when the page was last considered`)
    __obj.updateDynamic("Maximum page size seen")(`Maximum page size seen`)
    __obj.updateDynamic("Minimum on-disk page image size seen")(`Minimum on-disk page image size seen`)
    __obj.updateDynamic("On-disk page image sizes smaller than a single allocation unit")(`On-disk page image sizes smaller than a single allocation unit`)
    __obj.updateDynamic("Pages created in memory and never written")(`Pages created in memory and never written`)
    __obj.updateDynamic("Pages currently queued for eviction")(`Pages currently queued for eviction`)
    __obj.updateDynamic("Pages that could not be queued for eviction")(`Pages that could not be queued for eviction`)
    __obj.updateDynamic("Refs skipped during cache traversal")(`Refs skipped during cache traversal`)
    __obj.updateDynamic("Size of the root page")(`Size of the root page`)
    __obj.updateDynamic("Total number of pages currently in cache")(`Total number of pages currently in cache`)
    __obj.asInstanceOf[Anon_Averagedifferencebetweencurrentevictiongenerationwhenthepagewaslastconsidered]
  }
}

