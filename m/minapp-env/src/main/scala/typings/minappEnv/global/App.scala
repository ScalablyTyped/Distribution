package typings.minappEnv.global

import org.scalablytyped.runtime.Shortcut
import typings.minappEnv.App.AppConstructor
import org.scalablytyped.runtime.StObject
import scala.scalajs.js
import scala.scalajs.js.annotation.{JSGlobalScope, JSGlobal, JSImport, JSName, JSBracketAccess}

object App extends Shortcut {
  
  @JSGlobal("App")
  @js.native
  val ^ : AppConstructor = js.native
  
  type _To = AppConstructor
  
  /* This means you don't have to write `^`, but can instead just say `App.foo` */
  override def _to: AppConstructor = ^
}
