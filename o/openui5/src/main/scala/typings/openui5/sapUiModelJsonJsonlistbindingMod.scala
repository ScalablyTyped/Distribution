package typings.openui5

import org.scalablytyped.runtime.StObject
import scala.scalajs.js
import scala.scalajs.js.annotation.{JSGlobalScope, JSGlobal, JSImport, JSName, JSBracketAccess}

object sapUiModelJsonJsonlistbindingMod {
  
  @JSImport("sap/ui/model/json/JSONListBinding", JSImport.Default)
  @js.native
  open class default protected ()
    extends typings.openui5.sapUiModelClientListBindingMod.default {
    /**
      * Protected:  Do not call from applications (only from related classes in the framework)
      *
      * Creates a new JSONListBinding.
      *
      * This constructor should only be called by subclasses or model implementations, not by application or
      * control code. Such code should use {@link sap.ui.model.json.JSONModel#bindList JSONModel#bindList} on
      * the corresponding model instance instead.
      */
    def this(
      /**
      * Model instance that this binding is created for and that it belongs to
      */
    oModel: typings.openui5.sapUiModelJsonJsonmodelMod.default,
      /**
      * Binding path to be used for this binding
      */
    sPath: String,
      /**
      * Binding context relative to which a relative binding path will be resolved
      */
    oContext: typings.openui5.sapUiModelContextMod.default
    ) = this()
    def this(
      /**
      * Model instance that this binding is created for and that it belongs to
      */
    oModel: typings.openui5.sapUiModelJsonJsonmodelMod.default,
      /**
      * Binding path to be used for this binding
      */
    sPath: String,
      /**
      * Binding context relative to which a relative binding path will be resolved
      */
    oContext: typings.openui5.sapUiModelContextMod.default,
      /**
      * Initial sort order (can be either a sorter or an array of sorters)
      */
    aSorters: js.Array[typings.openui5.sapUiModelSorterMod.default]
    ) = this()
    def this(
      /**
      * Model instance that this binding is created for and that it belongs to
      */
    oModel: typings.openui5.sapUiModelJsonJsonmodelMod.default,
      /**
      * Binding path to be used for this binding
      */
    sPath: String,
      /**
      * Binding context relative to which a relative binding path will be resolved
      */
    oContext: typings.openui5.sapUiModelContextMod.default,
      /**
      * Initial sort order (can be either a sorter or an array of sorters)
      */
    aSorters: typings.openui5.sapUiModelSorterMod.default
    ) = this()
    def this(
      /**
      * Model instance that this binding is created for and that it belongs to
      */
    oModel: typings.openui5.sapUiModelJsonJsonmodelMod.default,
      /**
      * Binding path to be used for this binding
      */
    sPath: String,
      /**
      * Binding context relative to which a relative binding path will be resolved
      */
    oContext: typings.openui5.sapUiModelContextMod.default,
      /**
      * Initial sort order (can be either a sorter or an array of sorters)
      */
    aSorters: js.Array[typings.openui5.sapUiModelSorterMod.default],
      /**
      * Predefined filter/s (can be either a filter or an array of filters)
      */
    aFilters: js.Array[typings.openui5.sapUiModelFilterMod.default]
    ) = this()
    def this(
      /**
      * Model instance that this binding is created for and that it belongs to
      */
    oModel: typings.openui5.sapUiModelJsonJsonmodelMod.default,
      /**
      * Binding path to be used for this binding
      */
    sPath: String,
      /**
      * Binding context relative to which a relative binding path will be resolved
      */
    oContext: typings.openui5.sapUiModelContextMod.default,
      /**
      * Initial sort order (can be either a sorter or an array of sorters)
      */
    aSorters: js.Array[typings.openui5.sapUiModelSorterMod.default],
      /**
      * Predefined filter/s (can be either a filter or an array of filters)
      */
    aFilters: typings.openui5.sapUiModelFilterMod.default
    ) = this()
    def this(
      /**
      * Model instance that this binding is created for and that it belongs to
      */
    oModel: typings.openui5.sapUiModelJsonJsonmodelMod.default,
      /**
      * Binding path to be used for this binding
      */
    sPath: String,
      /**
      * Binding context relative to which a relative binding path will be resolved
      */
    oContext: typings.openui5.sapUiModelContextMod.default,
      /**
      * Initial sort order (can be either a sorter or an array of sorters)
      */
    aSorters: Unit,
      /**
      * Predefined filter/s (can be either a filter or an array of filters)
      */
    aFilters: js.Array[typings.openui5.sapUiModelFilterMod.default]
    ) = this()
    def this(
      /**
      * Model instance that this binding is created for and that it belongs to
      */
    oModel: typings.openui5.sapUiModelJsonJsonmodelMod.default,
      /**
      * Binding path to be used for this binding
      */
    sPath: String,
      /**
      * Binding context relative to which a relative binding path will be resolved
      */
    oContext: typings.openui5.sapUiModelContextMod.default,
      /**
      * Initial sort order (can be either a sorter or an array of sorters)
      */
    aSorters: Unit,
      /**
      * Predefined filter/s (can be either a filter or an array of filters)
      */
    aFilters: typings.openui5.sapUiModelFilterMod.default
    ) = this()
    def this(
      /**
      * Model instance that this binding is created for and that it belongs to
      */
    oModel: typings.openui5.sapUiModelJsonJsonmodelMod.default,
      /**
      * Binding path to be used for this binding
      */
    sPath: String,
      /**
      * Binding context relative to which a relative binding path will be resolved
      */
    oContext: typings.openui5.sapUiModelContextMod.default,
      /**
      * Initial sort order (can be either a sorter or an array of sorters)
      */
    aSorters: typings.openui5.sapUiModelSorterMod.default,
      /**
      * Predefined filter/s (can be either a filter or an array of filters)
      */
    aFilters: js.Array[typings.openui5.sapUiModelFilterMod.default]
    ) = this()
    def this(
      /**
      * Model instance that this binding is created for and that it belongs to
      */
    oModel: typings.openui5.sapUiModelJsonJsonmodelMod.default,
      /**
      * Binding path to be used for this binding
      */
    sPath: String,
      /**
      * Binding context relative to which a relative binding path will be resolved
      */
    oContext: typings.openui5.sapUiModelContextMod.default,
      /**
      * Initial sort order (can be either a sorter or an array of sorters)
      */
    aSorters: typings.openui5.sapUiModelSorterMod.default,
      /**
      * Predefined filter/s (can be either a filter or an array of filters)
      */
    aFilters: typings.openui5.sapUiModelFilterMod.default
    ) = this()
    def this(
      /**
      * Model instance that this binding is created for and that it belongs to
      */
    oModel: typings.openui5.sapUiModelJsonJsonmodelMod.default,
      /**
      * Binding path to be used for this binding
      */
    sPath: String,
      /**
      * Binding context relative to which a relative binding path will be resolved
      */
    oContext: typings.openui5.sapUiModelContextMod.default,
      /**
      * Initial sort order (can be either a sorter or an array of sorters)
      */
    aSorters: js.Array[typings.openui5.sapUiModelSorterMod.default],
      /**
      * Predefined filter/s (can be either a filter or an array of filters)
      */
    aFilters: js.Array[typings.openui5.sapUiModelFilterMod.default],
      /**
      * Map of optional parameters as defined by subclasses; this class does not introduce any own parameters
      */
    mParameters: js.Object
    ) = this()
    def this(
      /**
      * Model instance that this binding is created for and that it belongs to
      */
    oModel: typings.openui5.sapUiModelJsonJsonmodelMod.default,
      /**
      * Binding path to be used for this binding
      */
    sPath: String,
      /**
      * Binding context relative to which a relative binding path will be resolved
      */
    oContext: typings.openui5.sapUiModelContextMod.default,
      /**
      * Initial sort order (can be either a sorter or an array of sorters)
      */
    aSorters: js.Array[typings.openui5.sapUiModelSorterMod.default],
      /**
      * Predefined filter/s (can be either a filter or an array of filters)
      */
    aFilters: Unit,
      /**
      * Map of optional parameters as defined by subclasses; this class does not introduce any own parameters
      */
    mParameters: js.Object
    ) = this()
    def this(
      /**
      * Model instance that this binding is created for and that it belongs to
      */
    oModel: typings.openui5.sapUiModelJsonJsonmodelMod.default,
      /**
      * Binding path to be used for this binding
      */
    sPath: String,
      /**
      * Binding context relative to which a relative binding path will be resolved
      */
    oContext: typings.openui5.sapUiModelContextMod.default,
      /**
      * Initial sort order (can be either a sorter or an array of sorters)
      */
    aSorters: js.Array[typings.openui5.sapUiModelSorterMod.default],
      /**
      * Predefined filter/s (can be either a filter or an array of filters)
      */
    aFilters: typings.openui5.sapUiModelFilterMod.default,
      /**
      * Map of optional parameters as defined by subclasses; this class does not introduce any own parameters
      */
    mParameters: js.Object
    ) = this()
    def this(
      /**
      * Model instance that this binding is created for and that it belongs to
      */
    oModel: typings.openui5.sapUiModelJsonJsonmodelMod.default,
      /**
      * Binding path to be used for this binding
      */
    sPath: String,
      /**
      * Binding context relative to which a relative binding path will be resolved
      */
    oContext: typings.openui5.sapUiModelContextMod.default,
      /**
      * Initial sort order (can be either a sorter or an array of sorters)
      */
    aSorters: Unit,
      /**
      * Predefined filter/s (can be either a filter or an array of filters)
      */
    aFilters: js.Array[typings.openui5.sapUiModelFilterMod.default],
      /**
      * Map of optional parameters as defined by subclasses; this class does not introduce any own parameters
      */
    mParameters: js.Object
    ) = this()
    def this(
      /**
      * Model instance that this binding is created for and that it belongs to
      */
    oModel: typings.openui5.sapUiModelJsonJsonmodelMod.default,
      /**
      * Binding path to be used for this binding
      */
    sPath: String,
      /**
      * Binding context relative to which a relative binding path will be resolved
      */
    oContext: typings.openui5.sapUiModelContextMod.default,
      /**
      * Initial sort order (can be either a sorter or an array of sorters)
      */
    aSorters: Unit,
      /**
      * Predefined filter/s (can be either a filter or an array of filters)
      */
    aFilters: Unit,
      /**
      * Map of optional parameters as defined by subclasses; this class does not introduce any own parameters
      */
    mParameters: js.Object
    ) = this()
    def this(
      /**
      * Model instance that this binding is created for and that it belongs to
      */
    oModel: typings.openui5.sapUiModelJsonJsonmodelMod.default,
      /**
      * Binding path to be used for this binding
      */
    sPath: String,
      /**
      * Binding context relative to which a relative binding path will be resolved
      */
    oContext: typings.openui5.sapUiModelContextMod.default,
      /**
      * Initial sort order (can be either a sorter or an array of sorters)
      */
    aSorters: Unit,
      /**
      * Predefined filter/s (can be either a filter or an array of filters)
      */
    aFilters: typings.openui5.sapUiModelFilterMod.default,
      /**
      * Map of optional parameters as defined by subclasses; this class does not introduce any own parameters
      */
    mParameters: js.Object
    ) = this()
    def this(
      /**
      * Model instance that this binding is created for and that it belongs to
      */
    oModel: typings.openui5.sapUiModelJsonJsonmodelMod.default,
      /**
      * Binding path to be used for this binding
      */
    sPath: String,
      /**
      * Binding context relative to which a relative binding path will be resolved
      */
    oContext: typings.openui5.sapUiModelContextMod.default,
      /**
      * Initial sort order (can be either a sorter or an array of sorters)
      */
    aSorters: typings.openui5.sapUiModelSorterMod.default,
      /**
      * Predefined filter/s (can be either a filter or an array of filters)
      */
    aFilters: js.Array[typings.openui5.sapUiModelFilterMod.default],
      /**
      * Map of optional parameters as defined by subclasses; this class does not introduce any own parameters
      */
    mParameters: js.Object
    ) = this()
    def this(
      /**
      * Model instance that this binding is created for and that it belongs to
      */
    oModel: typings.openui5.sapUiModelJsonJsonmodelMod.default,
      /**
      * Binding path to be used for this binding
      */
    sPath: String,
      /**
      * Binding context relative to which a relative binding path will be resolved
      */
    oContext: typings.openui5.sapUiModelContextMod.default,
      /**
      * Initial sort order (can be either a sorter or an array of sorters)
      */
    aSorters: typings.openui5.sapUiModelSorterMod.default,
      /**
      * Predefined filter/s (can be either a filter or an array of filters)
      */
    aFilters: Unit,
      /**
      * Map of optional parameters as defined by subclasses; this class does not introduce any own parameters
      */
    mParameters: js.Object
    ) = this()
    def this(
      /**
      * Model instance that this binding is created for and that it belongs to
      */
    oModel: typings.openui5.sapUiModelJsonJsonmodelMod.default,
      /**
      * Binding path to be used for this binding
      */
    sPath: String,
      /**
      * Binding context relative to which a relative binding path will be resolved
      */
    oContext: typings.openui5.sapUiModelContextMod.default,
      /**
      * Initial sort order (can be either a sorter or an array of sorters)
      */
    aSorters: typings.openui5.sapUiModelSorterMod.default,
      /**
      * Predefined filter/s (can be either a filter or an array of filters)
      */
    aFilters: typings.openui5.sapUiModelFilterMod.default,
      /**
      * Map of optional parameters as defined by subclasses; this class does not introduce any own parameters
      */
    mParameters: js.Object
    ) = this()
  }
  
  type JSONListBinding = typings.openui5.sapUiModelClientListBindingMod.default
}
