package typings.forgeViewer.global.THREE

import org.scalablytyped.runtime.StObject
import scala.scalajs.js
import scala.scalajs.js.annotation.{JSGlobalScope, JSGlobal, JSImport, JSName, JSBracketAccess}

@JSGlobal("THREE.BoxGeometry")
@js.native
open class BoxGeometry protected ()
  extends StObject
     with typings.forgeViewer.THREE.BoxGeometry {
  def this(width: Double, height: Double, depth: Double) = this()
  def this(width: Double, height: Double, depth: Double, widthSegments: Double) = this()
  def this(width: Double, height: Double, depth: Double, widthSegments: Double, heightSegments: Double) = this()
  def this(width: Double, height: Double, depth: Double, widthSegments: Unit, heightSegments: Double) = this()
  def this(
    width: Double,
    height: Double,
    depth: Double,
    widthSegments: Double,
    heightSegments: Double,
    depthSegments: Double
  ) = this()
  def this(
    width: Double,
    height: Double,
    depth: Double,
    widthSegments: Double,
    heightSegments: Unit,
    depthSegments: Double
  ) = this()
  def this(
    width: Double,
    height: Double,
    depth: Double,
    widthSegments: Unit,
    heightSegments: Double,
    depthSegments: Double
  ) = this()
  def this(
    width: Double,
    height: Double,
    depth: Double,
    widthSegments: Unit,
    heightSegments: Unit,
    depthSegments: Double
  ) = this()
}
