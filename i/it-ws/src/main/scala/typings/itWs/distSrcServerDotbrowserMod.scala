package typings.itWs

import org.scalablytyped.runtime.Shortcut
import org.scalablytyped.runtime.StObject
import scala.scalajs.js
import scala.scalajs.js.annotation.{JSGlobalScope, JSGlobal, JSImport, JSName, JSBracketAccess}

object distSrcServerDotbrowserMod extends Shortcut {
  
  @JSImport("it-ws/dist/src/server.browser", JSImport.Default)
  @js.native
  val default: /* is `Null`, but independent javascript fields cannot be in scala 3 */ Any = js.native
  
  type _To = /* is `Null`, but independent javascript fields cannot be in scala 3 */ Any
  
  /* This means you don't have to write `default`, but can instead just say `distSrcServerDotbrowserMod.foo` */
  override def _to: /* is `Null`, but independent javascript fields cannot be in scala 3 */ Any = default
}
