package typings.azure.mod

import org.scalablytyped.runtime.StObject
import scala.scalajs.js
import scala.scalajs.js.`|`
import scala.scalajs.js.annotation.{JSGlobalScope, JSGlobal, JSImport, JSName, JSBracketAccess}

@JSImport("azure", "ExponentialRetryPolicyFilter")
@js.native
class ExponentialRetryPolicyFilter () extends Filter {
  def this(retryCount: Double) = this()
  def this(retryCount: js.UndefOr[scala.Nothing], retryInterval: Double) = this()
  def this(retryCount: Double, retryInterval: Double) = this()
  def this(
    retryCount: js.UndefOr[scala.Nothing],
    retryInterval: js.UndefOr[scala.Nothing],
    minRetryInterval: Double
  ) = this()
  def this(retryCount: js.UndefOr[scala.Nothing], retryInterval: Double, minRetryInterval: Double) = this()
  def this(retryCount: Double, retryInterval: js.UndefOr[scala.Nothing], minRetryInterval: Double) = this()
  def this(retryCount: Double, retryInterval: Double, minRetryInterval: Double) = this()
  def this(
    retryCount: js.UndefOr[scala.Nothing],
    retryInterval: js.UndefOr[scala.Nothing],
    minRetryInterval: js.UndefOr[scala.Nothing],
    maxRetryInterval: Double
  ) = this()
  def this(
    retryCount: js.UndefOr[scala.Nothing],
    retryInterval: js.UndefOr[scala.Nothing],
    minRetryInterval: Double,
    maxRetryInterval: Double
  ) = this()
  def this(
    retryCount: js.UndefOr[scala.Nothing],
    retryInterval: Double,
    minRetryInterval: js.UndefOr[scala.Nothing],
    maxRetryInterval: Double
  ) = this()
  def this(
    retryCount: js.UndefOr[scala.Nothing],
    retryInterval: Double,
    minRetryInterval: Double,
    maxRetryInterval: Double
  ) = this()
  def this(
    retryCount: Double,
    retryInterval: js.UndefOr[scala.Nothing],
    minRetryInterval: js.UndefOr[scala.Nothing],
    maxRetryInterval: Double
  ) = this()
  def this(
    retryCount: Double,
    retryInterval: js.UndefOr[scala.Nothing],
    minRetryInterval: Double,
    maxRetryInterval: Double
  ) = this()
  def this(
    retryCount: Double,
    retryInterval: Double,
    minRetryInterval: js.UndefOr[scala.Nothing],
    maxRetryInterval: Double
  ) = this()
  def this(retryCount: Double, retryInterval: Double, minRetryInterval: Double, maxRetryInterval: Double) = this()
  
  var maxRetryInterval: Double = js.native
  
  var minRetryInterval: Double = js.native
  
  var retryCount: Double = js.native
  
  var retryInterval: Double = js.native
}
