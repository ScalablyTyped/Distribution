package typings.antlr4

import typings.antlr4.tokenMod.TokenSourceTuple
import org.scalablytyped.runtime.StObject
import scala.scalajs.js
import scala.scalajs.js.annotation.{JSGlobalScope, JSGlobal, JSImport, JSName, JSBracketAccess}

object commonTokenMod {
  
  @JSImport("antlr4/CommonToken", JSImport.Default)
  @js.native
  open class default () extends CommonToken {
    def this(source: TokenSourceTuple) = this()
    def this(source: Unit, `type`: Double) = this()
    def this(source: TokenSourceTuple, `type`: Double) = this()
    def this(source: Unit, `type`: Double, channel: Double) = this()
    def this(source: Unit, `type`: Unit, channel: Double) = this()
    def this(source: TokenSourceTuple, `type`: Double, channel: Double) = this()
    def this(source: TokenSourceTuple, `type`: Unit, channel: Double) = this()
    def this(source: Unit, `type`: Double, channel: Double, start: Double) = this()
    def this(source: Unit, `type`: Double, channel: Unit, start: Double) = this()
    def this(source: Unit, `type`: Unit, channel: Double, start: Double) = this()
    def this(source: Unit, `type`: Unit, channel: Unit, start: Double) = this()
    def this(source: TokenSourceTuple, `type`: Double, channel: Double, start: Double) = this()
    def this(source: TokenSourceTuple, `type`: Double, channel: Unit, start: Double) = this()
    def this(source: TokenSourceTuple, `type`: Unit, channel: Double, start: Double) = this()
    def this(source: TokenSourceTuple, `type`: Unit, channel: Unit, start: Double) = this()
    def this(source: Unit, `type`: Double, channel: Double, start: Double, stop: Double) = this()
    def this(source: Unit, `type`: Double, channel: Double, start: Unit, stop: Double) = this()
    def this(source: Unit, `type`: Double, channel: Unit, start: Double, stop: Double) = this()
    def this(source: Unit, `type`: Double, channel: Unit, start: Unit, stop: Double) = this()
    def this(source: Unit, `type`: Unit, channel: Double, start: Double, stop: Double) = this()
    def this(source: Unit, `type`: Unit, channel: Double, start: Unit, stop: Double) = this()
    def this(source: Unit, `type`: Unit, channel: Unit, start: Double, stop: Double) = this()
    def this(source: Unit, `type`: Unit, channel: Unit, start: Unit, stop: Double) = this()
    def this(source: TokenSourceTuple, `type`: Double, channel: Double, start: Double, stop: Double) = this()
    def this(source: TokenSourceTuple, `type`: Double, channel: Double, start: Unit, stop: Double) = this()
    def this(source: TokenSourceTuple, `type`: Double, channel: Unit, start: Double, stop: Double) = this()
    def this(source: TokenSourceTuple, `type`: Double, channel: Unit, start: Unit, stop: Double) = this()
    def this(source: TokenSourceTuple, `type`: Unit, channel: Double, start: Double, stop: Double) = this()
    def this(source: TokenSourceTuple, `type`: Unit, channel: Double, start: Unit, stop: Double) = this()
    def this(source: TokenSourceTuple, `type`: Unit, channel: Unit, start: Double, stop: Double) = this()
    def this(source: TokenSourceTuple, `type`: Unit, channel: Unit, start: Unit, stop: Double) = this()
  }
  /* static members */
  object default {
    
    @JSImport("antlr4/CommonToken", "default.EMPTY_SOURCE")
    @js.native
    val EMPTY_SOURCE: js.Tuple2[Null, Null] = js.native
  }
  
  @js.native
  trait CommonToken
    extends typings.antlr4.tokenMod.default
}
