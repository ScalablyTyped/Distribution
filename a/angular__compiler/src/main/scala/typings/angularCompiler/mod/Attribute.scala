package typings.angularCompiler.mod

import org.scalablytyped.runtime.StObject
import scala.scalajs.js
import scala.scalajs.js.annotation.{JSGlobalScope, JSGlobal, JSImport, JSName, JSBracketAccess}

@JSImport("@angular/compiler", "Attribute")
@js.native
open class Attribute protected ()
  extends NodeWithI18n
     with Node2 {
  def this(name: String, value: String, sourceSpan: ParseSourceSpan) = this()
  def this(name: String, value: String, sourceSpan: ParseSourceSpan, keySpan: ParseSourceSpan) = this()
  def this(
    name: String,
    value: String,
    sourceSpan: ParseSourceSpan,
    keySpan: Unit,
    valueSpan: ParseSourceSpan
  ) = this()
  def this(
    name: String,
    value: String,
    sourceSpan: ParseSourceSpan,
    keySpan: ParseSourceSpan,
    valueSpan: ParseSourceSpan
  ) = this()
  def this(
    name: String,
    value: String,
    sourceSpan: ParseSourceSpan,
    keySpan: Unit,
    valueSpan: Unit,
    valueTokens: js.Array[InterpolatedAttributeToken]
  ) = this()
  def this(
    name: String,
    value: String,
    sourceSpan: ParseSourceSpan,
    keySpan: Unit,
    valueSpan: ParseSourceSpan,
    valueTokens: js.Array[InterpolatedAttributeToken]
  ) = this()
  def this(
    name: String,
    value: String,
    sourceSpan: ParseSourceSpan,
    keySpan: ParseSourceSpan,
    valueSpan: Unit,
    valueTokens: js.Array[InterpolatedAttributeToken]
  ) = this()
  def this(
    name: String,
    value: String,
    sourceSpan: ParseSourceSpan,
    keySpan: ParseSourceSpan,
    valueSpan: ParseSourceSpan,
    valueTokens: js.Array[InterpolatedAttributeToken]
  ) = this()
  def this(
    name: String,
    value: String,
    sourceSpan: ParseSourceSpan,
    keySpan: Unit,
    valueSpan: Unit,
    valueTokens: js.Array[InterpolatedAttributeToken],
    i18n: I18nMeta2
  ) = this()
  def this(
    name: String,
    value: String,
    sourceSpan: ParseSourceSpan,
    keySpan: Unit,
    valueSpan: Unit,
    valueTokens: Unit,
    i18n: I18nMeta2
  ) = this()
  def this(
    name: String,
    value: String,
    sourceSpan: ParseSourceSpan,
    keySpan: Unit,
    valueSpan: ParseSourceSpan,
    valueTokens: js.Array[InterpolatedAttributeToken],
    i18n: I18nMeta2
  ) = this()
  def this(
    name: String,
    value: String,
    sourceSpan: ParseSourceSpan,
    keySpan: Unit,
    valueSpan: ParseSourceSpan,
    valueTokens: Unit,
    i18n: I18nMeta2
  ) = this()
  def this(
    name: String,
    value: String,
    sourceSpan: ParseSourceSpan,
    keySpan: ParseSourceSpan,
    valueSpan: Unit,
    valueTokens: js.Array[InterpolatedAttributeToken],
    i18n: I18nMeta2
  ) = this()
  def this(
    name: String,
    value: String,
    sourceSpan: ParseSourceSpan,
    keySpan: ParseSourceSpan,
    valueSpan: Unit,
    valueTokens: Unit,
    i18n: I18nMeta2
  ) = this()
  def this(
    name: String,
    value: String,
    sourceSpan: ParseSourceSpan,
    keySpan: ParseSourceSpan,
    valueSpan: ParseSourceSpan,
    valueTokens: js.Array[InterpolatedAttributeToken],
    i18n: I18nMeta2
  ) = this()
  def this(
    name: String,
    value: String,
    sourceSpan: ParseSourceSpan,
    keySpan: ParseSourceSpan,
    valueSpan: ParseSourceSpan,
    valueTokens: Unit,
    i18n: I18nMeta2
  ) = this()
  
  val keySpan: js.UndefOr[ParseSourceSpan] = js.native
  
  var name: String = js.native
  
  var value: String = js.native
  
  var valueSpan: js.UndefOr[ParseSourceSpan] = js.native
  
  var valueTokens: js.UndefOr[js.Array[InterpolatedAttributeToken]] = js.native
}
