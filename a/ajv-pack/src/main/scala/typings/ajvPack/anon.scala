package typings.ajvPack

import typings.ajv.coreMod.Ajv
import typings.ajv.distTypesMod.AnySchema
import typings.ajv.distTypesMod.KeywordDefinition
import typings.ajvPack.ajvPackStrings.log
import org.scalablytyped.runtime.StObject
import scala.scalajs.js
import scala.scalajs.js.annotation.{JSGlobalScope, JSGlobal, JSImport, JSName, JSBracketAccess}

object anon {
  
  @js.native
  trait FnCall extends StObject {
    
    def apply(schema: js.Array[AnySchema]): Ajv = js.native
    def apply(schema: js.Array[AnySchema], // If array is passed, `key` will be ignored
    key: String): Ajv = js.native
    def apply(
      schema: js.Array[AnySchema],
      // If array is passed, `key` will be ignored
    key: String,
      // Optional schema key. Can be passed to `validate` method instead of schema object or id/ref. One schema per instance can have empty `id` and `key`.
    _meta: Boolean
    ): Ajv = js.native
    def apply(
      schema: js.Array[AnySchema],
      // If array is passed, `key` will be ignored
    key: String,
      // Optional schema key. Can be passed to `validate` method instead of schema object or id/ref. One schema per instance can have empty `id` and `key`.
    _meta: Boolean,
      // true if schema is a meta-schema. Used internally, addMetaSchema should be used instead.
    _validateSchema: Boolean
    ): Ajv = js.native
    def apply(
      schema: js.Array[AnySchema],
      // If array is passed, `key` will be ignored
    key: String,
      // Optional schema key. Can be passed to `validate` method instead of schema object or id/ref. One schema per instance can have empty `id` and `key`.
    _meta: Boolean,
      // true if schema is a meta-schema. Used internally, addMetaSchema should be used instead.
    _validateSchema: log
    ): Ajv = js.native
    def apply(
      schema: js.Array[AnySchema],
      // If array is passed, `key` will be ignored
    key: String,
      // Optional schema key. Can be passed to `validate` method instead of schema object or id/ref. One schema per instance can have empty `id` and `key`.
    _meta: Unit,
      // true if schema is a meta-schema. Used internally, addMetaSchema should be used instead.
    _validateSchema: Boolean
    ): Ajv = js.native
    def apply(
      schema: js.Array[AnySchema],
      // If array is passed, `key` will be ignored
    key: String,
      // Optional schema key. Can be passed to `validate` method instead of schema object or id/ref. One schema per instance can have empty `id` and `key`.
    _meta: Unit,
      // true if schema is a meta-schema. Used internally, addMetaSchema should be used instead.
    _validateSchema: log
    ): Ajv = js.native
    def apply(
      schema: js.Array[AnySchema],
      // If array is passed, `key` will be ignored
    key: Unit,
      // Optional schema key. Can be passed to `validate` method instead of schema object or id/ref. One schema per instance can have empty `id` and `key`.
    _meta: Boolean
    ): Ajv = js.native
    def apply(
      schema: js.Array[AnySchema],
      // If array is passed, `key` will be ignored
    key: Unit,
      // Optional schema key. Can be passed to `validate` method instead of schema object or id/ref. One schema per instance can have empty `id` and `key`.
    _meta: Boolean,
      // true if schema is a meta-schema. Used internally, addMetaSchema should be used instead.
    _validateSchema: Boolean
    ): Ajv = js.native
    def apply(
      schema: js.Array[AnySchema],
      // If array is passed, `key` will be ignored
    key: Unit,
      // Optional schema key. Can be passed to `validate` method instead of schema object or id/ref. One schema per instance can have empty `id` and `key`.
    _meta: Boolean,
      // true if schema is a meta-schema. Used internally, addMetaSchema should be used instead.
    _validateSchema: log
    ): Ajv = js.native
    def apply(
      schema: js.Array[AnySchema],
      // If array is passed, `key` will be ignored
    key: Unit,
      // Optional schema key. Can be passed to `validate` method instead of schema object or id/ref. One schema per instance can have empty `id` and `key`.
    _meta: Unit,
      // true if schema is a meta-schema. Used internally, addMetaSchema should be used instead.
    _validateSchema: Boolean
    ): Ajv = js.native
    def apply(
      schema: js.Array[AnySchema],
      // If array is passed, `key` will be ignored
    key: Unit,
      // Optional schema key. Can be passed to `validate` method instead of schema object or id/ref. One schema per instance can have empty `id` and `key`.
    _meta: Unit,
      // true if schema is a meta-schema. Used internally, addMetaSchema should be used instead.
    _validateSchema: log
    ): Ajv = js.native
    def apply(schema: AnySchema): Ajv = js.native
    def apply(schema: AnySchema, // If array is passed, `key` will be ignored
    key: String): Ajv = js.native
    def apply(
      schema: AnySchema,
      // If array is passed, `key` will be ignored
    key: String,
      // Optional schema key. Can be passed to `validate` method instead of schema object or id/ref. One schema per instance can have empty `id` and `key`.
    _meta: Boolean
    ): Ajv = js.native
    def apply(
      schema: AnySchema,
      // If array is passed, `key` will be ignored
    key: String,
      // Optional schema key. Can be passed to `validate` method instead of schema object or id/ref. One schema per instance can have empty `id` and `key`.
    _meta: Boolean,
      // true if schema is a meta-schema. Used internally, addMetaSchema should be used instead.
    _validateSchema: Boolean
    ): Ajv = js.native
    def apply(
      schema: AnySchema,
      // If array is passed, `key` will be ignored
    key: String,
      // Optional schema key. Can be passed to `validate` method instead of schema object or id/ref. One schema per instance can have empty `id` and `key`.
    _meta: Boolean,
      // true if schema is a meta-schema. Used internally, addMetaSchema should be used instead.
    _validateSchema: log
    ): Ajv = js.native
    def apply(
      schema: AnySchema,
      // If array is passed, `key` will be ignored
    key: String,
      // Optional schema key. Can be passed to `validate` method instead of schema object or id/ref. One schema per instance can have empty `id` and `key`.
    _meta: Unit,
      // true if schema is a meta-schema. Used internally, addMetaSchema should be used instead.
    _validateSchema: Boolean
    ): Ajv = js.native
    def apply(
      schema: AnySchema,
      // If array is passed, `key` will be ignored
    key: String,
      // Optional schema key. Can be passed to `validate` method instead of schema object or id/ref. One schema per instance can have empty `id` and `key`.
    _meta: Unit,
      // true if schema is a meta-schema. Used internally, addMetaSchema should be used instead.
    _validateSchema: log
    ): Ajv = js.native
    def apply(
      schema: AnySchema,
      // If array is passed, `key` will be ignored
    key: Unit,
      // Optional schema key. Can be passed to `validate` method instead of schema object or id/ref. One schema per instance can have empty `id` and `key`.
    _meta: Boolean
    ): Ajv = js.native
    def apply(
      schema: AnySchema,
      // If array is passed, `key` will be ignored
    key: Unit,
      // Optional schema key. Can be passed to `validate` method instead of schema object or id/ref. One schema per instance can have empty `id` and `key`.
    _meta: Boolean,
      // true if schema is a meta-schema. Used internally, addMetaSchema should be used instead.
    _validateSchema: Boolean
    ): Ajv = js.native
    def apply(
      schema: AnySchema,
      // If array is passed, `key` will be ignored
    key: Unit,
      // Optional schema key. Can be passed to `validate` method instead of schema object or id/ref. One schema per instance can have empty `id` and `key`.
    _meta: Boolean,
      // true if schema is a meta-schema. Used internally, addMetaSchema should be used instead.
    _validateSchema: log
    ): Ajv = js.native
    def apply(
      schema: AnySchema,
      // If array is passed, `key` will be ignored
    key: Unit,
      // Optional schema key. Can be passed to `validate` method instead of schema object or id/ref. One schema per instance can have empty `id` and `key`.
    _meta: Unit,
      // true if schema is a meta-schema. Used internally, addMetaSchema should be used instead.
    _validateSchema: Boolean
    ): Ajv = js.native
    def apply(
      schema: AnySchema,
      // If array is passed, `key` will be ignored
    key: Unit,
      // Optional schema key. Can be passed to `validate` method instead of schema object or id/ref. One schema per instance can have empty `id` and `key`.
    _meta: Unit,
      // true if schema is a meta-schema. Used internally, addMetaSchema should be used instead.
    _validateSchema: log
    ): Ajv = js.native
  }
  
  @js.native
  trait FnCallKwdOrDefDef extends StObject {
    
    def apply(kwdOrDef: String): Ajv = js.native
    def apply(kwdOrDef: String, `def`: KeywordDefinition): Ajv = js.native
    def apply(kwdOrDef: KeywordDefinition): Ajv = js.native
    def apply(kwdOrDef: KeywordDefinition, `def`: KeywordDefinition): Ajv = js.native
  }
}
