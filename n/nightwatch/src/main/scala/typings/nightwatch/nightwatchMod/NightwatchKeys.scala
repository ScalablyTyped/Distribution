package typings.nightwatch.nightwatchMod

import scala.scalajs.js
import scala.scalajs.js.`|`
import scala.scalajs.js.annotation._

trait NightwatchKeys extends js.Object {
  /** The numpad add (+) key. */
  var ADD: String
  /** The alt key. */
  var ALT: String
  /** The down arrow. */
  var ARROW_DOWN: String
  /** The left arrow. */
  var ARROW_LEFT: String
  /** The right arrow. */
  var ARROW_RIGHT: String
  /** The up arrow. */
  var ARROW_UP: String
  /** The backspace key. */
  var BACK_SPACE: String
  /** OS-specific keystroke sequence that performs a cancel action. */
  var CANCEL: String
  /** The clear key. This key only appears on full-size Apple keyboards in place of Num Lock key. */
  var CLEAR: String
  /** The command (⌘) key. */
  var COMMAND: String
  /** The control key. */
  var CONTROL: String
  /** The numpad decimal (.) key. */
  var DECIMAL: String
  /** The delete key. */
  var DELETE: String
  /** The numpad divide (/) key. */
  var DIVIDE: String
  var DOWN_ARROW: String
  /** The end key. */
  var END: String
  /** The enter (numpad) key. */
  var ENTER: String
  /** The equals key. */
  var EQUALS: String
  /** The escape key. */
  var ESCAPE: String
  /** The F1 key. */
  var F1: String
  /** The F10 key. */
  var F10: String
  /** The F11 key. */
  var F11: String
  /** The F12 key. */
  var F12: String
  /** The F2 key. */
  var F2: String
  /** The F3 key. */
  var F3: String
  /** The F4 key. */
  var F4: String
  /** The F5 key. */
  var F5: String
  /** The F6 key. */
  var F6: String
  /** The F7 key. */
  var F7: String
  /** The F8 key. */
  var F8: String
  /** The F9 key. */
  var F9: String
  /** The help key. This key only appears on older Apple keyboards in place of the Insert key. */
  var HELP: String
  /** The home key. */
  var HOME: String
  /** The insert key. */
  var INSERT: String
  var LEFT_ARROW: String
  /** The meta (Windows) key. */
  var META: String
  /** The numpad multiply (*) key. */
  var MULTIPLY: String
  /** Releases all held modifier keys. */
  var NULL: String
  /** The numpad zero key. */
  var NUMPAD0: String
  /** The numpad one key. */
  var NUMPAD1: String
  /** The numpad two key. */
  var NUMPAD2: String
  /** The numpad three key. */
  var NUMPAD3: String
  /** The numpad four key. */
  var NUMPAD4: String
  /** The numpad five key. */
  var NUMPAD5: String
  /** The numpad six key. */
  var NUMPAD6: String
  /** The numpad seven key. */
  var NUMPAD7: String
  /** The numpad eight key. */
  var NUMPAD8: String
  /** The numpad nine key. */
  var NUMPAD9: String
  /** The page down key. */
  var PAGEDOWN: String
  /** The page up key. */
  var PAGEUP: String
  /** The pause key. */
  var PAUSE: String
  /** The return key. */
  var RETURN: String
  var RIGHT_ARROW: String
  /** The semicolon key. */
  var SEMICOLON: String
  /** The numpad separator (=) key. */
  var SEPARATOR: String
  /** The shift key. */
  var SHIFT: String
  /** The space bar. */
  var SPACE: String
  /** The numpad subtract (-) key. */
  var SUBTRACT: String
  /** The tab key. */
  var TAB: String
  var UP_ARROW: String
}

object NightwatchKeys {
  @scala.inline
  def apply(
    ADD: String,
    ALT: String,
    ARROW_DOWN: String,
    ARROW_LEFT: String,
    ARROW_RIGHT: String,
    ARROW_UP: String,
    BACK_SPACE: String,
    CANCEL: String,
    CLEAR: String,
    COMMAND: String,
    CONTROL: String,
    DECIMAL: String,
    DELETE: String,
    DIVIDE: String,
    DOWN_ARROW: String,
    END: String,
    ENTER: String,
    EQUALS: String,
    ESCAPE: String,
    F1: String,
    F10: String,
    F11: String,
    F12: String,
    F2: String,
    F3: String,
    F4: String,
    F5: String,
    F6: String,
    F7: String,
    F8: String,
    F9: String,
    HELP: String,
    HOME: String,
    INSERT: String,
    LEFT_ARROW: String,
    META: String,
    MULTIPLY: String,
    NULL: String,
    NUMPAD0: String,
    NUMPAD1: String,
    NUMPAD2: String,
    NUMPAD3: String,
    NUMPAD4: String,
    NUMPAD5: String,
    NUMPAD6: String,
    NUMPAD7: String,
    NUMPAD8: String,
    NUMPAD9: String,
    PAGEDOWN: String,
    PAGEUP: String,
    PAUSE: String,
    RETURN: String,
    RIGHT_ARROW: String,
    SEMICOLON: String,
    SEPARATOR: String,
    SHIFT: String,
    SPACE: String,
    SUBTRACT: String,
    TAB: String,
    UP_ARROW: String
  ): NightwatchKeys = {
    val __obj = js.Dynamic.literal(ADD = ADD, ALT = ALT, ARROW_DOWN = ARROW_DOWN, ARROW_LEFT = ARROW_LEFT, ARROW_RIGHT = ARROW_RIGHT, ARROW_UP = ARROW_UP, BACK_SPACE = BACK_SPACE, CANCEL = CANCEL, CLEAR = CLEAR, COMMAND = COMMAND, CONTROL = CONTROL, DECIMAL = DECIMAL, DELETE = DELETE, DIVIDE = DIVIDE, DOWN_ARROW = DOWN_ARROW, END = END, ENTER = ENTER, EQUALS = EQUALS, ESCAPE = ESCAPE, F1 = F1, F10 = F10, F11 = F11, F12 = F12, F2 = F2, F3 = F3, F4 = F4, F5 = F5, F6 = F6, F7 = F7, F8 = F8, F9 = F9, HELP = HELP, HOME = HOME, INSERT = INSERT, LEFT_ARROW = LEFT_ARROW, META = META, MULTIPLY = MULTIPLY, NULL = NULL, NUMPAD0 = NUMPAD0, NUMPAD1 = NUMPAD1, NUMPAD2 = NUMPAD2, NUMPAD3 = NUMPAD3, NUMPAD4 = NUMPAD4, NUMPAD5 = NUMPAD5, NUMPAD6 = NUMPAD6, NUMPAD7 = NUMPAD7, NUMPAD8 = NUMPAD8, NUMPAD9 = NUMPAD9, PAGEDOWN = PAGEDOWN, PAGEUP = PAGEUP, PAUSE = PAUSE, RETURN = RETURN, RIGHT_ARROW = RIGHT_ARROW, SEMICOLON = SEMICOLON, SEPARATOR = SEPARATOR, SHIFT = SHIFT, SPACE = SPACE, SUBTRACT = SUBTRACT, TAB = TAB, UP_ARROW = UP_ARROW)
  
    __obj.asInstanceOf[NightwatchKeys]
  }
}

