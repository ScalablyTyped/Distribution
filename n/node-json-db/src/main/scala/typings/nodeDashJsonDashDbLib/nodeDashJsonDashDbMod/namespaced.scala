package typings
package nodeDashJsonDashDbLib.nodeDashJsonDashDbMod

import scala.scalajs.js
import scala.scalajs.js.`|`
import scala.scalajs.js.annotation._

@JSImport("node-json-db", JSImport.Namespace)
@js.native
class namespaced protected () extends JsonDB {
  /**
       * Create the JSON database
       * @param filename where to save the data base
       * @param saveOnPush saving on modification of the data
       * @param humanReadable is the json file humand readable 
       * @returns {JsonDB}
       * @constructor
       */
  def this(filename: java.lang.String) = this()
  /**
       * Create the JSON database
       * @param filename where to save the data base
       * @param saveOnPush saving on modification of the data
       * @param humanReadable is the json file humand readable 
       * @returns {JsonDB}
       * @constructor
       */
  def this(filename: java.lang.String, saveOnPush: scala.Boolean) = this()
  /**
       * Create the JSON database
       * @param filename where to save the data base
       * @param saveOnPush saving on modification of the data
       * @param humanReadable is the json file humand readable 
       * @returns {JsonDB}
       * @constructor
       */
  def this(filename: java.lang.String, saveOnPush: scala.Boolean, humanReadable: scala.Boolean) = this()
}

