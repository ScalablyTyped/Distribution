package typings
package tabrisLib.JSXNs

import scala.scalajs.js
import scala.scalajs.js.`|`
import scala.scalajs.js.annotation._

trait IntrinsicElements extends js.Object {
  var action: ActionProperties
  var actionSheet: ActionSheetProperties
  var activityIndicator: ActivityIndicatorProperties
  var alertDialog: AlertDialogProperties
  var app: AppProperties
  var button: ButtonProperties
  var canvas: CanvasProperties
  var checkBox: CheckBoxProperties
  var collectionView: CollectionViewProperties
  var composite: CompositeProperties
  var dateDialog: DateDialogProperties
  var device: DeviceProperties
  var drawer: DrawerProperties
  var fileSystem: FileSystemProperties
  var imageView: ImageViewProperties
  var inactivityTimer: InactivityTimerProperties
  var nativeObject: NativeObjectProperties
  var navigationBar: NavigationBarProperties
  var navigationView: NavigationViewProperties
  var page: PageProperties
  var picker: PickerProperties
  var popover: PopoverProperties
  var popup: PopupProperties
  var printer: PrinterProperties
  var progressBar: ProgressBarProperties
  var radioButton: RadioButtonProperties
  var refreshComposite: RefreshCompositeProperties
  var scrollView: ScrollViewProperties
  var searchAction: SearchActionProperties
  var slider: SliderProperties
  var statusBar: StatusBarProperties
  var switch: SwitchProperties
  var tab: TabProperties
  var tabFolder: TabFolderProperties
  var textInput: TextInputProperties
  var textView: TextViewProperties
  var timeDialog: TimeDialogProperties
  var toggleButton: ToggleButtonProperties
  var uI: UIProperties
  var video: VideoProperties
  var webView: WebViewProperties
  var widget: WidgetProperties
}

object IntrinsicElements {
  @scala.inline
  def apply(
    action: ActionProperties,
    actionSheet: ActionSheetProperties,
    activityIndicator: ActivityIndicatorProperties,
    alertDialog: AlertDialogProperties,
    app: AppProperties,
    button: ButtonProperties,
    canvas: CanvasProperties,
    checkBox: CheckBoxProperties,
    collectionView: CollectionViewProperties,
    composite: CompositeProperties,
    dateDialog: DateDialogProperties,
    device: DeviceProperties,
    drawer: DrawerProperties,
    fileSystem: FileSystemProperties,
    imageView: ImageViewProperties,
    inactivityTimer: InactivityTimerProperties,
    nativeObject: NativeObjectProperties,
    navigationBar: NavigationBarProperties,
    navigationView: NavigationViewProperties,
    page: PageProperties,
    picker: PickerProperties,
    popover: PopoverProperties,
    popup: PopupProperties,
    printer: PrinterProperties,
    progressBar: ProgressBarProperties,
    radioButton: RadioButtonProperties,
    refreshComposite: RefreshCompositeProperties,
    scrollView: ScrollViewProperties,
    searchAction: SearchActionProperties,
    slider: SliderProperties,
    statusBar: StatusBarProperties,
    switch: SwitchProperties,
    tab: TabProperties,
    tabFolder: TabFolderProperties,
    textInput: TextInputProperties,
    textView: TextViewProperties,
    timeDialog: TimeDialogProperties,
    toggleButton: ToggleButtonProperties,
    uI: UIProperties,
    video: VideoProperties,
    webView: WebViewProperties,
    widget: WidgetProperties
  ): IntrinsicElements = {
    val __obj = js.Dynamic.literal(action = action, actionSheet = actionSheet, activityIndicator = activityIndicator, alertDialog = alertDialog, app = app, button = button, canvas = canvas, checkBox = checkBox, collectionView = collectionView, composite = composite, dateDialog = dateDialog, device = device, drawer = drawer, fileSystem = fileSystem, imageView = imageView, inactivityTimer = inactivityTimer, nativeObject = nativeObject, navigationBar = navigationBar, navigationView = navigationView, page = page, picker = picker, popover = popover, popup = popup, printer = printer, progressBar = progressBar, radioButton = radioButton, refreshComposite = refreshComposite, scrollView = scrollView, searchAction = searchAction, slider = slider, statusBar = statusBar, switch = switch, tab = tab, tabFolder = tabFolder, textInput = textInput, textView = textView, timeDialog = timeDialog, toggleButton = toggleButton, uI = uI, video = video, webView = webView, widget = widget)
  
    __obj.asInstanceOf[IntrinsicElements]
  }
}

