package typings.webcola.mod

import org.scalablytyped.runtime.StObject
import scala.scalajs.js
import scala.scalajs.js.annotation.{JSGlobalScope, JSGlobal, JSImport, JSName, JSBracketAccess}

@JSImport("webcola", "Module")
@js.native
open class Module protected ()
  extends typings.webcola.distSrcPowergraphMod.Module {
  def this(id: Double) = this()
  def this(id: Double, outgoing: typings.webcola.distSrcPowergraphMod.LinkSets) = this()
  def this(id: Double, outgoing: Unit, incoming: typings.webcola.distSrcPowergraphMod.LinkSets) = this()
  def this(
    id: Double,
    outgoing: typings.webcola.distSrcPowergraphMod.LinkSets,
    incoming: typings.webcola.distSrcPowergraphMod.LinkSets
  ) = this()
  def this(
    id: Double,
    outgoing: Unit,
    incoming: Unit,
    children: typings.webcola.distSrcPowergraphMod.ModuleSet
  ) = this()
  def this(
    id: Double,
    outgoing: Unit,
    incoming: typings.webcola.distSrcPowergraphMod.LinkSets,
    children: typings.webcola.distSrcPowergraphMod.ModuleSet
  ) = this()
  def this(
    id: Double,
    outgoing: typings.webcola.distSrcPowergraphMod.LinkSets,
    incoming: Unit,
    children: typings.webcola.distSrcPowergraphMod.ModuleSet
  ) = this()
  def this(
    id: Double,
    outgoing: typings.webcola.distSrcPowergraphMod.LinkSets,
    incoming: typings.webcola.distSrcPowergraphMod.LinkSets,
    children: typings.webcola.distSrcPowergraphMod.ModuleSet
  ) = this()
  def this(id: Double, outgoing: Unit, incoming: Unit, children: Unit, definition: Any) = this()
  def this(
    id: Double,
    outgoing: Unit,
    incoming: Unit,
    children: typings.webcola.distSrcPowergraphMod.ModuleSet,
    definition: Any
  ) = this()
  def this(
    id: Double,
    outgoing: Unit,
    incoming: typings.webcola.distSrcPowergraphMod.LinkSets,
    children: Unit,
    definition: Any
  ) = this()
  def this(
    id: Double,
    outgoing: Unit,
    incoming: typings.webcola.distSrcPowergraphMod.LinkSets,
    children: typings.webcola.distSrcPowergraphMod.ModuleSet,
    definition: Any
  ) = this()
  def this(
    id: Double,
    outgoing: typings.webcola.distSrcPowergraphMod.LinkSets,
    incoming: Unit,
    children: Unit,
    definition: Any
  ) = this()
  def this(
    id: Double,
    outgoing: typings.webcola.distSrcPowergraphMod.LinkSets,
    incoming: Unit,
    children: typings.webcola.distSrcPowergraphMod.ModuleSet,
    definition: Any
  ) = this()
  def this(
    id: Double,
    outgoing: typings.webcola.distSrcPowergraphMod.LinkSets,
    incoming: typings.webcola.distSrcPowergraphMod.LinkSets,
    children: Unit,
    definition: Any
  ) = this()
  def this(
    id: Double,
    outgoing: typings.webcola.distSrcPowergraphMod.LinkSets,
    incoming: typings.webcola.distSrcPowergraphMod.LinkSets,
    children: typings.webcola.distSrcPowergraphMod.ModuleSet,
    definition: Any
  ) = this()
}
