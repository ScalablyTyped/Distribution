package typings
package workboxDashSwLib

import scala.scalajs.js
import scala.scalajs.js.`|`
import scala.scalajs.js.annotation._

@js.native
trait Anon_CacheableResponse
  extends org.scalablytyped.runtime.Instantiable0[workboxDashSwLib.workboxDashSwMod.CacheableResponseNamespace] {
  /**
  	 * This class allows you to set up rules determining what
  	 * status codes and/or headers need to be present in order for a
  	 * [`Response`](https://developer.mozilla.org/en-US/docs/Web/API/Response)
  	 * to be considered cacheable.
  	 */
  val CacheableResponse: workboxDashSwLib.workboxDashSwMod.CacheableResponseConstructor = js.native
  /**
  	 * A class implementing the `cacheWillUpdate` lifecycle callback. This makes it
  	 * easier to add in cacheability checks to requests made via Workbox's built-in
  	 * strategies.
  	 */
  val Plugin: workboxDashSwLib.workboxDashSwMod.CacheableResponsePluginConstructor = js.native
}

