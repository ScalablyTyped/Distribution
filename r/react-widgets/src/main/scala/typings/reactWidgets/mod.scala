package typings.reactWidgets

import org.scalablytyped.runtime.Shortcut
import typings.react.mod.Component
import typings.react.mod.ComponentState
import typings.reactWidgets.calendarMod.CalendarClass
import typings.reactWidgets.calendarMod.CalendarProps
import typings.reactWidgets.comboboxMod.ComboboxClass
import typings.reactWidgets.comboboxMod.ComboboxProps
import typings.reactWidgets.dateTimePickerMod.DateTimePickerClass
import typings.reactWidgets.dateTimePickerMod.DateTimePickerProps
import typings.reactWidgets.dropdownListMod.DropdownListClass
import typings.reactWidgets.dropdownListMod.DropdownListProps
import typings.reactWidgets.multiselectMod.MultiselectClass
import typings.reactWidgets.multiselectMod.MultiselectProps
import typings.reactWidgets.numberPickerMod.NumberPickerClass
import typings.reactWidgets.numberPickerMod.NumberPickerProps
import typings.reactWidgets.selectListMod.SelectListClass
import typings.reactWidgets.selectListMod.SelectListProps
import org.scalablytyped.runtime.StObject
import scala.scalajs.js
import scala.scalajs.js.annotation.{JSGlobalScope, JSGlobal, JSImport, JSName, JSBracketAccess}

object mod {
  
  object Calendar extends Shortcut {
    
    /* This class was inferred from a value with a constructor. In rare cases (like HTMLElement in the DOM) it might not work as you expect. */
    @JSImport("react-widgets", "Calendar")
    @js.native
    class ^ protected ()
      extends Component[CalendarProps, ComponentState, js.Any] {
      def this(props: CalendarProps) = this()
      def this(props: CalendarProps, context: js.Any) = this()
    }
    
    @JSImport("react-widgets", "Calendar")
    @js.native
    val ^ : js.Object & CalendarClass = js.native
    
    type _To = js.Object & CalendarClass
    
    /* This means you don't have to write `^`, but can instead just say `Calendar.foo` */
    override def _to: js.Object & CalendarClass = ^
  }
  
  object Combobox extends Shortcut {
    
    /* This class was inferred from a value with a constructor. In rare cases (like HTMLElement in the DOM) it might not work as you expect. */
    @JSImport("react-widgets", "Combobox")
    @js.native
    class ^ protected ()
      extends Component[ComboboxProps, ComponentState, js.Any] {
      def this(props: ComboboxProps) = this()
      def this(props: ComboboxProps, context: js.Any) = this()
    }
    
    @JSImport("react-widgets", "Combobox")
    @js.native
    val ^ : js.Object & ComboboxClass = js.native
    
    type _To = js.Object & ComboboxClass
    
    /* This means you don't have to write `^`, but can instead just say `Combobox.foo` */
    override def _to: js.Object & ComboboxClass = ^
  }
  
  object DateTimePicker extends Shortcut {
    
    /* This class was inferred from a value with a constructor. In rare cases (like HTMLElement in the DOM) it might not work as you expect. */
    @JSImport("react-widgets", "DateTimePicker")
    @js.native
    class ^ protected ()
      extends Component[DateTimePickerProps, ComponentState, js.Any] {
      def this(props: DateTimePickerProps) = this()
      def this(props: DateTimePickerProps, context: js.Any) = this()
    }
    
    @JSImport("react-widgets", "DateTimePicker")
    @js.native
    val ^ : js.Object & DateTimePickerClass = js.native
    
    type _To = js.Object & DateTimePickerClass
    
    /* This means you don't have to write `^`, but can instead just say `DateTimePicker.foo` */
    override def _to: js.Object & DateTimePickerClass = ^
  }
  
  object DropdownList extends Shortcut {
    
    /* This class was inferred from a value with a constructor. In rare cases (like HTMLElement in the DOM) it might not work as you expect. */
    @JSImport("react-widgets", "DropdownList")
    @js.native
    class ^ protected ()
      extends Component[DropdownListProps, ComponentState, js.Any] {
      def this(props: DropdownListProps) = this()
      def this(props: DropdownListProps, context: js.Any) = this()
    }
    
    @JSImport("react-widgets", "DropdownList")
    @js.native
    val ^ : js.Object & DropdownListClass = js.native
    
    type _To = js.Object & DropdownListClass
    
    /* This means you don't have to write `^`, but can instead just say `DropdownList.foo` */
    override def _to: js.Object & DropdownListClass = ^
  }
  
  object Multiselect extends Shortcut {
    
    /* This class was inferred from a value with a constructor. In rare cases (like HTMLElement in the DOM) it might not work as you expect. */
    @JSImport("react-widgets", "Multiselect")
    @js.native
    class ^ protected ()
      extends Component[MultiselectProps, ComponentState, js.Any] {
      def this(props: MultiselectProps) = this()
      def this(props: MultiselectProps, context: js.Any) = this()
    }
    
    @JSImport("react-widgets", "Multiselect")
    @js.native
    val ^ : js.Object & MultiselectClass = js.native
    
    type _To = js.Object & MultiselectClass
    
    /* This means you don't have to write `^`, but can instead just say `Multiselect.foo` */
    override def _to: js.Object & MultiselectClass = ^
  }
  
  object NumberPicker extends Shortcut {
    
    /* This class was inferred from a value with a constructor. In rare cases (like HTMLElement in the DOM) it might not work as you expect. */
    @JSImport("react-widgets", "NumberPicker")
    @js.native
    class ^ protected ()
      extends Component[NumberPickerProps, ComponentState, js.Any] {
      def this(props: NumberPickerProps) = this()
      def this(props: NumberPickerProps, context: js.Any) = this()
    }
    
    @JSImport("react-widgets", "NumberPicker")
    @js.native
    val ^ : js.Object & NumberPickerClass = js.native
    
    type _To = js.Object & NumberPickerClass
    
    /* This means you don't have to write `^`, but can instead just say `NumberPicker.foo` */
    override def _to: js.Object & NumberPickerClass = ^
  }
  
  object SelectList extends Shortcut {
    
    /* This class was inferred from a value with a constructor. In rare cases (like HTMLElement in the DOM) it might not work as you expect. */
    @JSImport("react-widgets", "SelectList")
    @js.native
    class ^ protected ()
      extends Component[SelectListProps, ComponentState, js.Any] {
      def this(props: SelectListProps) = this()
      def this(props: SelectListProps, context: js.Any) = this()
    }
    
    @JSImport("react-widgets", "SelectList")
    @js.native
    val ^ : js.Object & SelectListClass = js.native
    
    type _To = js.Object & SelectListClass
    
    /* This means you don't have to write `^`, but can instead just say `SelectList.foo` */
    override def _to: js.Object & SelectListClass = ^
  }
}
